# 첫술 구현 현황 요약

`REQUIREMENTS.md` 기준으로 현재 프로젝트에서 구현된 기능과 앞으로 구현할 우선순위를 간단히 정리한 문서입니다.

상세 구현 현황은 아래 스프레드시트에서도 확인할 수 있습니다.

- [구현 현황 스프레드시트](https://docs.google.com/spreadsheets/d/11NdTIIhAw5m0TVwFOo6YXLfNDQcLLcFi8TeA7wp5uos/edit?usp=sharing)

## 1. 한눈에 보기

| 상태 | 개수 | 비율 |
| --- | ---: | ---: |
| 완료 | 41개 | 26.8% |
| 부분 완료 | 15개 | 9.8% |
| 미완료 | 97개 | 63.4% |
| 총합 | 153개 | 100% |

진행률:

| 기준 | 비율 |
| --- | ---: |
| 완료만 기준 | 26.8% |
| 완료 + 부분 완료 기준 | 36.6% |
| 남은 작업 | 63.4% |

## 2. 서비스 핵심 가치

이 서비스의 핵심 흐름은 아래와 같습니다.

```text
마트 / 편의점 / 술집
↓
술 바코드 스캔
↓
술 정보 확인
↓
리뷰 작성
↓
맛/향 데이터 축적
↓
검색 및 추천
```

따라서 구현 우선순위는 단순 회원 기능보다 **리뷰, Taste/Scent 데이터 축적, 검색/추천 기반**을 먼저 완성하는 방향으로 잡습니다.

## 3. 현재 완료된 기능

| 영역 | 완료된 기능 |
| --- | --- |
| 회원 | 회원가입, 로그인, 아이디 중복 확인, 닉네임 중복 확인, 회원 조회 |
| 주류 | 주류 전체 목록 조회, 주류 번호 조회, 바코드 조회, 주류 등록 |
| 맛/향 | `Taste`, `Scent` 엔티티와 Repository 존재, 주류와 Taste/Scent 연결 저장 |
| 리뷰 | 특정 주류 리뷰 조회, 일반 리뷰 등록, 리뷰 별점 필드 존재 |
| 평점 | `Alcohol.avgStar` 필드 존재 |
| 포인트/등급 | `Member.point`, `Member.grade` 필드 존재 |
| 테이스팅 노트 | `Tastenote` 엔티티와 Service 일부 존재 |

## 4. 현재 부족한 핵심 기능

| 영역 | 부족한 점 |
| --- | --- |
| Taste 데이터 축적 | 리뷰 작성 시 Taste를 선택하고 저장하는 구조가 없음 |
| Taste 목록 API | `GET /tastes` API가 없음 |
| Scent 목록 API | `GET /scents` API가 없음 |
| 리뷰-Taste 연결 | `ReviewTaste` 테이블 또는 관계가 없음 |
| 리뷰-Scent 연결 | `ReviewScent` 테이블 또는 관계가 없음 |
| Taste/Scent 검색 | 맛/향 기준 주류 검색 API가 없음 |
| 대표 Taste 통계 | 사용자 리뷰 기반 Taste 집계 로직이 없음 |
| 평균 별점 | 리뷰 작성 후 `Alcohol.avgStar` 자동 계산이 없음 |
| 정렬 | 별점순, 리뷰순 정렬 API가 없음 |
| 미등록 바코드 | 바코드 조회 실패 시 등록 요청으로 이어지는 구조가 없음 |
| 관리자 승인 | 사용자 등록 요청을 검수하고 Alcohol로 승인하는 기능이 없음 |
| 인증 | JWT 인증, 로그아웃, 내 리뷰 조회가 없음 |

## 5. 구현 우선순위

### P0 - 핵심 데이터 축적

가장 먼저 해야 할 작업입니다. Taste/Scent 데이터가 쌓이지 않으면 검색, 추천, 통계 기능을 만들 수 없습니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 1 | 리뷰 작성 시 Taste 선택 및 `ReviewTaste` 저장 | 미완료 |
| 2 | Taste 목록 조회 API `GET /tastes` | 미완료 |
| 3 | 리뷰 작성 시 Scent 선택 및 `ReviewScent` 저장 | 미완료 |
| 4 | Scent 목록 조회 API `GET /scents` | 미완료 |

### P1 - 데이터 활용

축적한 Taste/Scent 데이터를 사용자 가치로 연결하는 단계입니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 5 | Taste 기반 주류 검색 `GET /alcohols/search?taste=` | 미완료 |
| 6 | Scent 기반 주류 검색 `GET /alcohols/search?scent=` | 미완료 |
| 7 | 주류 대표 Taste 통계 | 미완료 |
| 8 | 주류 대표 Scent 통계 | 미완료 |

### P2 - 데이터 확보

바코드 기반 서비스의 데이터 부족 문제를 해결하는 단계입니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 9 | 미등록 바코드 등록 요청 API | 미완료 |
| 10 | 등록 요청 엔티티 생성 | 미완료 |
| 11 | 관리자 승인/거절 기능 | 미완료 |
| 12 | 승인 시 `Alcohol` 등록 | 미완료 |
| 13 | `Alcohol.barcode` unique 제약 | 미완료 |

### P3 - 사용자 경험

사용자가 술을 더 잘 비교하고 찾을 수 있게 하는 기능입니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 14 | 리뷰 저장 시 평균 별점 계산 | 미완료 |
| 15 | 별점순 정렬 | 미완료 |
| 16 | 리뷰순 정렬 | 미완료 |
| 17 | 리뷰 글자 수 검증 | 미완료 |

### P4 - 사용자 관리

리뷰와 개인 데이터를 안정적으로 운영하기 위한 기반입니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 18 | JWT 인증 | 미완료 |
| 19 | 로그아웃 | 미완료 |
| 20 | 내 리뷰 조회 | 미완료 |
| 21 | 공개 노트 조회 | 미완료 |

### P5 - 부가 기능

핵심 데이터 흐름이 완성된 뒤 구현할 기능입니다.

| 순서 | 작업 | 현재 상태 |
| ---: | --- | --- |
| 22 | 포인트 지급 | 부분 완료 |
| 23 | 등급 자동 갱신 | 부분 완료 |
| 24 | 리뷰 좋아요 | 미완료 |
| 25 | Q&A | 미완료 |
| 26 | 소셜 로그인 | 미완료 |
| 27 | 회원가입 입력값 검증 | 미완료 |

## 6. 다음 개발 추천 순서

바로 개발을 시작한다면 아래 순서가 가장 좋습니다.

1. `GET /tastes` API 만들기
2. 리뷰 작성 요청에 `tasteIds` 추가
3. `ReviewTaste` 엔티티/테이블 만들기
4. 리뷰 저장 시 Taste 연결 저장
5. `GET /scents` API 만들기
6. 리뷰 작성 요청에 `scentIds` 추가
7. Taste/Scent 기반 주류 검색 만들기
8. 평균 별점 자동 계산 추가
9. 미등록 바코드 등록 요청 구조 만들기
10. 관리자 승인 후 Alcohol 등록 구조 만들기

## 7. 결론

현재 프로젝트는 주류 조회, 회원가입, 로그인, 리뷰 작성 같은 기본 API는 갖춰져 있습니다. 하지만 서비스의 핵심 자산이 될 **리뷰 기반 Taste/Scent 데이터 축적 구조**가 아직 없습니다.

따라서 다음 개발의 1순위는 회원 부가 기능이 아니라, 리뷰 작성 시 Taste/Scent를 선택하고 저장하는 구조를 만드는 것입니다.
